# Lambda production dependencies (excluding sipap-common and dev dependencies)
# sipap-common will be bundled directly from source

# HTTP client for API requests (API-based architecture v2.3)
aiohttp>=3.9.0
certifi>=2024.0.0

# Database & Cache
asyncpg>=0.29.0
redis[hiredis]>=5.0.0

# Data processing
pydantic>=2.5.0
python-dateutil>=2.8.2

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0
